# Template — values populated by k8s/setup.sh or kubectl create secret
apiVersion: v1
kind: Secret
metadata:
  name: n184-api-keys
  namespace: n184
  labels:
    app.kubernetes.io/part-of: n184
type: Opaque
stringData:
  ANTHROPIC_API_KEY: ""
  TELEGRAM_BOT_TOKEN: ""
  # Optional additional providers
  # DEEPSEEK_API_KEY: ""
  # OPENAI_API_KEY: ""
